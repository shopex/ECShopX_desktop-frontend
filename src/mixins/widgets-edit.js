export default {
  // layout: ({ route }) => {
  //   return route.path.indexOf('/shopadmin') > -1 ? 'edit' : 'default'
  // }
  layout: ( app ) => {
    return "../../custome/layouts/index";
    // return "index"
  }
};
